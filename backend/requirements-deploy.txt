# 用于 Railway 部署（CPU-only，版本兼容）
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20

Pillow==11.2.1
numpy

# CPU-only torch（通过 Dockerfile 单独安装，见 Dockerfile）
# torch 和 torchvision 不在此处，避免下载 CUDA 版

transformers==5.1.0
huggingface_hub==1.4.1
accelerate==1.12.0
safetensors==0.7.0

opencv-contrib-python-headless
einops
kornia
timm
